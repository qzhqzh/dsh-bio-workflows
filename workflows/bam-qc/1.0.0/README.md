# BAM quality control

Runs `samtools flagstat` and `samtools stats` for one BAM file and returns both
text reports. The path in `examples/inputs.json` is a placeholder and must be
replaced before an engine run.

This starter is structurally checked by `dsh-bio-workflows`; it has not yet
crossed the package's execution or engine-validation boundary.
